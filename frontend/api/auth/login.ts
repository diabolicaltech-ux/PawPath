import type { VercelRequest, VercelResponse } from '@vercel/node';
import crypto from 'node:crypto';
export default function handler(req: VercelRequest,res: VercelResponse){const id=process.env.GOOGLE_CLIENT_ID;if(!id)return res.status(503).send('Google login is not configured.');const state=crypto.randomBytes(24).toString('hex');const redirect=process.env.GOOGLE_REDIRECT_URI||'https://pawpath.quest/api/auth/callback';const q=new URLSearchParams({client_id:id,redirect_uri:redirect,response_type:'code',scope:'openid email profile',prompt:'select_account',state});res.setHeader('Set-Cookie',`oauth_state=${state}; HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=600`);res.redirect(`https://accounts.google.com/o/oauth2/v2/auth?${q}`)}
